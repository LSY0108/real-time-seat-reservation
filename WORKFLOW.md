# WORKFLOW.md — 개발 작업 규칙 / 절차

이 저장소에서 작업하는 모든 AI 에이전트(Claude Code 등)에 적용되는 규칙이다. 레포 구조/도메인 규칙은 [`AGENTS.md`](AGENTS.md) 참고 — 이 파일은 "어떻게 작업을 진행하는가"만 다룬다.

---

## 개발 작업 규칙 (필수, 항상 우선 적용)

### 1. 구현 전 승인

- 사용자의 명시적인 승인 전에는 구현, 코드 작성, 파일 수정/생성/삭제를 시작하지 않는다.
- 먼저 작업 계획과 변경 범위를 설명하고 승인을 받은 후 구현한다.
- 요구사항이 불명확하면 구현하지 말고 질문한다.

### 2. 수정 범위 제한

- 승인받은 파일과 범위를 벗어난 수정은 하지 않는다.
- 추가 수정이 필요하면 이유와 영향을 설명한 후 승인을 받는다.
- 무관한 코드, 기존 기능, 다른 역할의 코드는 수정하지 않는다.
- 리팩터링, 포맷팅, 네이밍 변경은 승인 없이 수행하지 않는다.

### 3. 데이터베이스

- DB를 직접 수정하지 않는다.
- 이 프로젝트는 별도 마이그레이션 도구 없이 Hibernate `ddl-auto: create`로 스키마를 관리한다 — 엔티티 변경이 곧 스키마 변경이므로, 엔티티 구조를 바꿀 때도 영향 범위를 설명하고 승인을 받는다.
- 운영 데이터 변경, 수동 SQL 실행, (향후 마이그레이션 도구 도입 시) Migration 생성이 필요하면 왜 필요한지 / 영향 범위 / 위험 요소 / 롤백 방법을 설명한 후 승인을 받는다.

### 4. Git

다음 작업은 승인 전 수행하지 않는다: `commit`, `push`, `pull`, `merge`, `rebase`, branch 생성/변경/삭제, tag 생성. 필요하면 커밋 메시지만 제안한다. 사용자가 별도로 명시적으로 요청한 경우에만 예외.

커밋 메시지에 `Co-Authored-By: Claude ...` 같은 AI 공동저자 표시를 넣지 않는다 — GitHub 커밋 작성자에 AI가 노출되지 않길 원함. 이 저장소뿐 아니라 기본값으로 항상 적용한다.

### 5. 기존 코드 우선

새로운 구조를 만들기 전에 기존 API, DTO, Entity, Service, Util, Component, Hook, 공통 모듈을 먼저 확인하고, 재사용 가능한 코드가 있으면 우선 사용한다. 더 좋은 구조가 있다면 이유·장점·단점·기존 방식과의 차이를 설명하고 선택지를 제안한다.

### 6. 영향 최소화

기존 기능, 기존 테스트, 다른 역할의 코드, 다른 모듈에 대한 영향을 항상 최소화한다. 특히 Redis 키 구조, HOLD/confirm 동시성 로직, 인증 토큰 흐름처럼 건드리면 파급 범위가 큰 지점은 계획 단계에서 리스크로 명시한다. Breaking Change가 발생하면 반드시 사전에 설명한다.

### 7. 코드 품질

작업 완료 전 임시 코드, Debug 로그, 사용하지 않는 코드, TODO를 정리한다.

### 8. 보고 방식

항상 **사실**(확인된 내용만) / **추론**(가능성 있는 내용) / **미검증**(아직 확인 못한 내용)을 구분해서 보고한다. 추측을 사실처럼 작성하지 않는다.

---

## 작업 절차

새 기능/변경 사항을 요청받으면 아래 순서를 따른다. 순서를 건너뛰고 바로 구현부터 시작하지 말 것. 경미한 변경(오탈자, 문서 링크 수정 등)의 절차 간소화는 아래 "경미한 변경 예외" 참고.

1. **요구사항 분석** — 포트폴리오 모드가 필요한 작업이면(아래 "포트폴리오 모드" 참고) 여기서 목표(백엔드/프론트엔드/풀스택/미정)를 먼저 확인한다.
2. **수정 대상 파일** 특정
3. **변경 범위** 정리 — 브랜치명(예: `feat/xxx`), 영향받는 모듈, 테스트 전략(어떤 시나리오를 어느 테스트 클래스에 추가할지, 동시성 검증이 필요한지) 포함
4. **구현 방법** 제시 — 대안이 있으면 방법 1/방법 2로 나눠 장단점 설명
5. **사용자 승인** — 승인 전 구현하지 않는다
6. **구현** — 코드 작성 및 도메인 문서(`CLAUDE.md` 등) 갱신 — 사용자가 볼 만한 변경(기능 추가/제거, 동작 변경)이면 `CHANGELOG.md`의 `[Unreleased]`에도 한 줄 추가한다 (상세 기준은 [`AGENTS.md`](AGENTS.md#문서-갱신-원칙)의 "문서 갱신 원칙" 참고). 수정이 여러 라운드로 이어질 것 같으면(구현 → 검증 → 실패 → 재수정 반복) `/loop`(Claude Code) 등 반복 실행 도구로 fix→검증→fix 사이클을 세션 내에서 자동 반복시킬 수 있다 — 세션이 끝나면 사라지는 일시적 도구이며 별도 인프라(CI 등)를 구성하는 것이 아니다. **단, 루프 도중 승인받은 범위를 벗어나는 수정(리팩터링, 구조 변경, 관련 없는 파일 수정 등)이 필요하다고 판단되면 루프를 즉시 멈추고 사용자에게 범위 확장을 설명한 후 재승인을 받는다 — 루프의 자동성이 "2. 수정 범위 제한" 규칙의 예외가 되지 않는다.**
7. **검증** — 구현이 끝났다고 바로 완료 처리하지 않는다. `.claude/skills/verify/SKILL.md`(Claude Code 기준. 다른 툴은 해당 파일을 그대로 읽고 절차를 수동으로 따른다)의 절차대로 실제로 띄워서 관찰하고 PASS/FAIL/BLOCKED/SKIP 중 하나로 판정한다. FAIL/BLOCKED면 Step 6으로 돌아가 재수정한다(루프 사용 시 위 예외 규칙 적용).
8. **결과 보고** — 변경 파일 / 변경 내용 / 영향 범위 / 검증 방법과 결과(Step 7의 판정)를 정리하고, 커밋 메시지(초안)를 제안한다(실제 커밋/푸시는 하지 않음, 위 "4. Git" 참고).
9. **회고** — 예상과 달랐던 점, 다음에 참고할 점을 짧게 남긴다. 포트폴리오 모드였다면 이 단계에서 `PORTFOLIO.md`(재사용한 코드/새로 만든 코드/설계 이유/학습 노트/면접 대비)를 채운다.

## 경미한 변경 예외

오탈자 수정, 문서 링크 정정, 주석 수정처럼 동작에 영향 없고 되돌리기 쉬운 변경은 위 9단계 전체를 거치지 않고 바로 수정 후 무엇을 바꿨는지 한 줄로 보고해도 된다. 판단이 애매하면(예: "이 정도면 경미한가?") 경미한 변경으로 취급하지 말고 정식 절차를 따른다.

## 포트폴리오 모드

**활성화 조건**: 사용자가 명시적으로 요청했거나, 새로운 기능 구현·설계 결정이 포함된 작업일 때 Step 1에서 활성화 여부를 확인한다. 오탈자 수정, 문서 정리, 사소한 버그 픽스처럼 설계 판단이 없는 작업에는 적용하지 않는다.

재사용한 코드/새로 만든 코드/설계 이유/학습 노트/면접 대비 같은 커리어용 기록은 `PORTFOLIO.md`(저장소 루트, Git에 커밋되지 않음 — 개인 기록용) 참고. 구현 사실과 커리어 어필은 반드시 분리한다: `PORTFOLIO.md`에도 과장하거나 실제 하지 않은 내용은 적지 않는다.